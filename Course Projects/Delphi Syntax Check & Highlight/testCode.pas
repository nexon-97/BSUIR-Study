Program testCode;

{
 Multiline comment example
}

Uses SysUtils;

const HELLO_MSSAGE : String = 'Bullshit';
      otherConst   : Word   = 5;

var i: LongInt;

const Bitch = 'BITCH';

Some code; // single line comment

begin
 WriteLn(HELLO_MESSAGE);

	for i := 0 to 50 do
		SomeTestAction;

 No lags now, quick, nice editor;

	ReadLn; // Wait for user input
end.
